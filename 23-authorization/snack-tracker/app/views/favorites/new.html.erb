<h1>Choose Your Favorites</h1>

<%= form_tag yummy_index_path do %>
    <% Snack.all.each do |s| %>
        <div>
            <%= check_box_tag 'snack_ids[]', s.id %>
            <%= s.name %>
        </div>
    <% end %>
    <%= submit_tag "Submit Favorites" %>
<% end %>
